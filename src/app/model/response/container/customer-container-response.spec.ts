import { CustomerContainerResponse } from './customer-container-response';

describe('CustomerContainerResponse', () => {
  it('should create an instance', () => {
    expect(new CustomerContainerResponse()).toBeTruthy();
  });
});
