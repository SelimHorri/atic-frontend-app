import { ApiPayloadReservation } from './api-payload-reservation';

describe('ApiPayloadReservation', () => {
  it('should create an instance', () => {
    expect(new ApiPayloadReservation()).toBeTruthy();
  });
});
