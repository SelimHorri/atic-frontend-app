import { ApiPayloadSaloonList } from './api-payload-saloon-list';

describe('ApiPayloadSaloonList', () => {
  it('should create an instance', () => {
    expect(new ApiPayloadSaloonList()).toBeTruthy();
  });
});
