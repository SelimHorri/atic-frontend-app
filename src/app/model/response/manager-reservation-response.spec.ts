import { ManagerReservationResponse } from './manager-reservation-response';

describe('ManagerReservationResponse', () => {
  it('should create an instance', () => {
    expect(new ManagerReservationResponse()).toBeTruthy();
  });
});
