import { ReservationContainerResponse } from './reservation-container-response';

describe('ReservationContainerResponse', () => {
  it('should create an instance', () => {
    expect(new ReservationContainerResponse()).toBeTruthy();
  });
});
