import { ReservationBeginEndTaskResponse } from './reservation-begin-end-task-response';

describe('ReservationBeginEndTaskResponse', () => {
  it('should create an instance', () => {
    expect(new ReservationBeginEndTaskResponse()).toBeTruthy();
  });
});
