import { CustomerProfileResponse } from './customer-profile-response';

describe('CustomerProfileResponse', () => {
  it('should create an instance', () => {
    expect(new CustomerProfileResponse()).toBeTruthy();
  });
});
