import { CustomerReservationResponse } from './customer-reservation-response';

describe('CustomerReservationResponse', () => {
  it('should create an instance', () => {
    expect(new CustomerReservationResponse()).toBeTruthy();
  });
});
