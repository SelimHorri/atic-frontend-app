import { TaskBeginEndRequest } from './task-begin-end-request';

describe('TaskBeginEndRequest', () => {
  it('should create an instance', () => {
    expect(new TaskBeginEndRequest()).toBeTruthy();
  });
});
