import { OrderedDetailRequest } from './ordered-detail-request';

describe('OrderedDetailRequest', () => {
  it('should create an instance', () => {
    expect(new OrderedDetailRequest()).toBeTruthy();
  });
});
