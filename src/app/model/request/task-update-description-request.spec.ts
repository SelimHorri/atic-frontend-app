import { TaskUpdateDescriptionRequest } from './task-update-description-request';

describe('TaskUpdateDescriptionRequest', () => {
  it('should create an instance', () => {
    expect(new TaskUpdateDescriptionRequest()).toBeTruthy();
  });
});
