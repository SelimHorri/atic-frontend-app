import { ReservationRequest } from './reservation-request';

describe('ReservationRequest', () => {
  it('should create an instance', () => {
    expect(new ReservationRequest()).toBeTruthy();
  });
});
