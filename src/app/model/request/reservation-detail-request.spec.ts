import { ReservationDetailRequest } from './reservation-detail-request';

describe('ReservationDetailRequest', () => {
  it('should create an instance', () => {
    expect(new ReservationDetailRequest()).toBeTruthy();
  });
});
