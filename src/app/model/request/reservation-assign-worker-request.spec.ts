import { ReservationAssignWorkerRequest } from './reservation-assign-worker-request';

describe('ReservationAssignWorkerRequest', () => {
  it('should create an instance', () => {
    expect(new ReservationAssignWorkerRequest()).toBeTruthy();
  });
});
